import { NgSortableHeader } from './sortable.directive';

describe('SortableDirective', () => {
  it('should create an instance', () => {
    const directive = new NgSortableHeader();
    expect(directive).toBeTruthy();
  });
});
